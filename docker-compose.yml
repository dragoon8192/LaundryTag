version: '0.1'
services:
  tensorflow-gpu:
    restart: always
    build: .
    container_name: tensorflow-gpu
    working_dir: '/root/src'
    tty: true
    volumes:
      - ./src:/root/src
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
